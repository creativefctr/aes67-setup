# Python dependencies for PTP-enabled GStreamer sender
# Install with: pip install -r requirements.txt

# GStreamer Python bindings (PyGObject)
# Provides access to GStreamer C API including PTP clock support
PyGObject>=3.42.0

